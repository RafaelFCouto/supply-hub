services:
  postgres:
    image: postgres:16-alpine
    container_name: supply-hub-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: supply_hub_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d supply_hub_db']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
